<?php
   $num_one = 1;
   $num_two = 2;
   $num_three = 3;

   // var_dump($num_one);
   // var_dump(1);
   // var_dump('1');
   //
   // $total = var_dump($num_one * $num_three);
   // echo $total;

   $distanceHome = 2.3;
   $distanceWork = 4.8;

   var_dump($distanceHome + $distanceWork / $num_three);

   $a = 5;
   $b = 10;
   var_dump($a * $b);
   var_dump($a / $b);

   $a++; // increment operator
   $a--; // decrement operator
   $a += 5;
   $a -= 5;

?>
